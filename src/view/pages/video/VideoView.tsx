import App from "../../../App";

export function VideoView(props: any) {
    return (App(page()))
}

function page() {
    return (
        <main>
            <h1>VideoView</h1>
        </main>
    )
}
